assets:
  # Required read/write S3 credentials for private development images. No defaults;
  # restricted to postlude-images-dev in the EU. Never used by CI or PR previews.
  images_rw:
    access_key_id: set-by-standards-creds-add-cloudflare
    secret_access_key: set-by-standards-creds-add-cloudflare
  screenshots_rw:
    access_key_id: replace-with-r2-access-key-id
    secret_access_key: replace-with-r2-secret-access-key
