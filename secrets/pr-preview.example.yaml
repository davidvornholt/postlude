# Plaintext shape only. Do not put real preview credentials in this file.
# The protected `pr-preview` GitHub environment can decrypt the matching SOPS
# target, which contains only the forced-command SSH key.
ci:
  ssh_private_key: |-
    -----BEGIN OPENSSH PRIVATE KEY-----
    replace-with-the-dedicated-postlude-preview-deploy-key
    -----END OPENSSH PRIVATE KEY-----
