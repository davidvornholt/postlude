ALTER TABLE "entry" ADD COLUMN "search_vector" "tsvector" GENERATED ALWAYS AS (to_tsvector('simple', coalesce(journal_markdown, '') || ' ' || coalesce(scripture_markdown, '') || ' ' || coalesce(scripture_book, ''))) STORED;--> statement-breakpoint
CREATE INDEX "entry_search_vector_index" ON "entry" USING gin ("search_vector");