import type { SearchPage as SearchPageComponent } from '../src/features/journal/ui/search-page.tsx';

export const SearchPage: typeof SearchPageComponent = () => {
  throw new Error('The search-page fixture must be loaded through Vite.');
};
